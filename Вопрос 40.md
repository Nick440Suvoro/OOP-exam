# Вопрос 40
**Перегрузка операций сравнения `==`, `!=`, `<`, `<=`, `>`, `>=` и её особенности.**

* Следует начать с того, что все операторы сравнения являются **бинарными** операторами, которые не изменяют свои **левые** операнды.

[//]: <> (а правые изменяют?)

```c++
class Integer
{
private:
    int value;
public:
    Integer(int i) : value(i) {}

    Integer(const Integer& copy) : value(copy.value) {}

    friend bool operator== (const Integer &I_1, const Integer &I_2);
    friend bool operator!= (const Integer &I_1, const Integer &I_2);
};
```
Далее переопределим данные операторы.
```c++
bool operator== (const Integer &I_1, const Integer &I_2)
{
    return (I_1.value == I_2.value);
}
 
bool operator!= (const Integer &I_1, const Integer &I_2)
{
    return !(I_1.value == I_2.value);
}
```

[//]: <> (в заголовке у тебя есть отметка про особенности, так и какие у них особенности перегрузки? )